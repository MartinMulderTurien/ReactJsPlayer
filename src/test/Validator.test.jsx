import { expect }  from 'chai';
import * as Validator from '../js/common/Validator';
describe("Validator", function() {
    it("should exist",function() {
        expect(Validator).to.exist;
    });
});